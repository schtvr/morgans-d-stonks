# frontend

Next.js dashboard for the portfolio platform.

## Getting Started

```bash
npm install
npm run dev
```

The app expects the backend API URL and auth/session behavior configured through the repo root `.env` and Docker Compose defaults.
