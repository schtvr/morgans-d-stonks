# Dev-only overrides (volume mounts, hot reload).
services:
  web:
    volumes:
      - ./frontend:/app
      - /app/node_modules
    # Keep the container-only node_modules volume in sync when package-lock changes.
    command: sh -c "npm install && npm run dev"
